services:
  mysql:
    image: mysql:8.0
    container_name: logiroute_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: "123456"
      MYSQL_DATABASE: "LogiRoute"
    ports:
      - "3310:3306"
    volumes:
      - ./logiroute.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
